id: recipe_ebf3_gun_column_optical_axis_datum_set
kind: recipe
variant_id: ebf3_route_review_v0
target_item_id: ebf3_gun_column_optical_axis_datum_set
inputs:
- item_id: steel_bar_raw
  qty: 0.2625
  unit: kg
outputs:
- item_id: ebf3_gun_column_optical_axis_datum_set
  qty: 1.0
  unit: unit
steps:
- process_id: machining_basic_v0
  inputs:
  - item_id: steel_bar_raw
    qty: 0.2625
    unit: kg
  outputs:
  - item_id: ebf3_gun_column_optical_axis_datum_set
    qty: 1.0
    unit: unit
  notes: Machine the datum geometry and reference surfaces; alignment transfer remains a flagged setup
    check.
notes: First-pass local machining route. Residual inspection, calibration, or parent-level qualification
  is recorded on the item as a non-blocking verification gap.

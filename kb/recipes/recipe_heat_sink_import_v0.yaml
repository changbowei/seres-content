id: recipe_heat_sink_import_v0
target_item_id: heat_sink
variant_id: import_v0
steps:
- process_id: heat_sink_extrusion_v0
  inputs:
  - item_id: aluminum_alloy_ingot
    qty: 1.0
    unit: kg
  outputs:
  - item_id: heat_sink
    qty: 0.15
    unit: unit
  est_time_hr: 0.3
  notes: Extrude aluminum alloy ingot into heat sink profile.
assumptions: |
  Heat sink is produced via extrusion; losses to scrap are not explicitly modeled here.
notes: "Replace import placeholder with a real production path."

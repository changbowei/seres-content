id: mock_assembly_v0
name: Mock assembly process for gap resolution v0
kind: process
inputs:
- item_id: atomization_unit_v0
  qty: 0.0
  unit: unit
outputs:
- item_id: atomization_unit_v0
  qty: 1.0
  unit: unit
resource_requirements:
- machine_id: labor_bot_general_v0
  qty: 1
  unit: count
energy_model:
  type: fixed_per_batch
  value: 0.5
  unit: kWh
time_model:
  type: batch
  hr_per_batch: 1.0
process_type: batch
notes: Placeholder mock assembly to close no_recipe gap for atomization_unit_v0; inputs
  empty or placeholder; use conservative estimates.

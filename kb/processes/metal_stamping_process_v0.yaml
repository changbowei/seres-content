id: metal_stamping_process_v0
kind: process
name: Metal stamping process v0
layer_tags:
- layer_5
inputs:
- item_id: steel_sheet_1mm
  qty: 0.1
  unit: kg
outputs:
- item_id: breaker_housing_and_mechanism
  qty: 0.09
  unit: kg
resource_requirements:
- machine_id: labor_bot_general_v0
  qty: 1
  unit: count
- machine_id: stamping_press_basic
  qty: 1
  unit: count
energy_model:
  type: fixed_per_batch
  value: 0.04
  unit: kWh
time_model:
  type: batch
  hr_per_batch: 0.25
notes: Template stamping/punching operation for sheet-metal parts; concrete recipes override inputs and
  outputs. Part-specific die tooling is required but not modeled as a machine resource in the current
  schema.
process_type: batch
future_improvements:
- Add a schema-supported non-consumed tooling requirement for punch_die_set or part-specific dies; do
  not hide die needs inside labor.
- Replace generic punch_die_set with part-specific die/tooling when stamped EBF parts dominate local manufacturing
  conclusions.
- Audit stamping_press_basic capabilities before treating stamped routes as clean local manufacturing.
is_template: true
